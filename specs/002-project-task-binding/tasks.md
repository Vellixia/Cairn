# Tasks: Project and Task Binding Foundation

**Input**: Authoritative design documents from `specs/002-project-task-binding/`:
`spec.md`, `plan.md`, `research.md`, `data-model.md`, `migration-design.md`,
`event-catalog.md`, `module-ownership.md`, `compatibility-matrix.md`,
`testing-evidence.md`, `quickstart.md`, and `contracts/`.

**Tests**: Required by the specification. Story tests are written before their
implementation tasks and must initially fail for the missing behavior.

**Organization**: Tasks preserve Feature 001 first, establish shared domain/storage
foundations, deliver User Stories 1–5 in dependency order, then collect exact-commit
evidence and run the final convergence gate.


**Authoritative task count after analysis remediation**: 136. All are intentionally unchecked before implementation.

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Safe to execute concurrently after its stated prerequisites because it uses
  different files and does not depend on another incomplete parallel task.
- **[US1]…[US5]**: Maps to the prioritized user stories in `spec.md`.
- Every task cites its requirement, scenario, success criterion, plan decision, or
  constitution principle.
- A checked box is never evidence by itself; the named test, migration, command, or
  completed workflow must pass.

---

## Phase 1: Workspace and Feature 001 Compatibility Baseline

**Purpose**: Prove and freeze the inherited local foundation before Feature 002 changes
session, storage, event, IPC, or CLI behavior.

**Checkpoint**: Do not begin Feature 002 runtime work until T001–T007 pass and the
baseline fixture is trustworthy.

- [X] T001 Run `cargo test --workspace --all-targets` plus `cargo test -p cairn-daemon --test us2_agent_sim`, `us3_tracking`, and `us3_events`; record exact commands, commit SHA, environment, and results in `specs/002-project-task-binding/evidence/feature-001-baseline.md` (FR-052, compatibility matrix)
- [X] T002 Verify repository/worktree identity, snapshots, sessions, leases, watcher readiness, post-install reconciliation, interruption, recovery, append-only events, replay, secure IPC, and CLI JSON behavior using the existing Feature 001 suites; map each passing suite to the compatibility matrix in `specs/002-project-task-binding/evidence/feature-001-compatibility.md` (FR-022, FR-032, Constitution I/IV)
- [X] T003 Capture the exact Feature 001 `0001_init.sql` checksum and runtime `sqlite_master` tables, columns, indexes, and triggers in `specs/002-project-task-binding/evidence/feature-001-schema-baseline.md` without changing `crates/cairn-storage-local/migrations/0001_init.sql` (FR-033, Plan Phase 1)
- [X] T004 From a clean checkout of Feature 001 commit `4a06c4125715bb4b78b54e49c81eccd82100a7b7`, create a populated database containing representative repositories, worktrees, snapshots, ordered events, leases, token hashes, and sessions in every lifecycle state at `fixtures/databases/feature-001-v1.sqlite3` (FR-034, SC-003)
- [X] T005 Create `fixtures/databases/feature-001-v1.manifest.json` and `fixtures/databases/README.md` with producer SHA, fixture hash, schema versions, object names, table counts, ordered event-row hashes, session fields, and an assertion that no raw token or secret is present (FR-034, FR-051, migration design)
- [X] T006 Add fixture-origin and manifest verification in `crates/cairn-storage-local/tests/feature001_fixture.rs`, failing if the fixture was generated by Feature 002 schema/code or if any manifest hash/count differs (SC-003, Constitution I/IV)
- [X] T007 Complete the baseline decision record in `specs/002-project-task-binding/evidence/feature-001-compatibility.md`; leave this task open for any failing Feature 001 suite or unexplained schema/fixture mismatch (FR-052, Constitution IV)
- [X] T008 Add the focused `crates/cairn-project/Cargo.toml`, `crates/cairn-project/src/lib.rs`, and root `Cargo.toml` workspace entries with only the dependencies allowed by `module-ownership.md` (Plan domain ownership, Constitution IX)
- [X] T009 Add deterministic Feature 002 repository/database builders and independent-pool helpers in `crates/cairn-storage-local/tests/support/mod.rs` and `apps/daemon/tests/support/feature002.rs` without changing Feature 001 fixture semantics (testing strategy, Constitution IV)

---

## Phase 2: Domain, Event, and Protocol Foundations

**Purpose**: Establish shared typed invariants before migrations or user-story services.

**Checkpoint**: Binding mode is orthogonal to lifecycle state, all IDs and errors are
typed, and no untyped JSON escape hatch exists.

- [X] T010 Add UUIDv7 `ProjectId`, `ProjectRepositoryAssociationId`, `TaskId`, `TaskRevisionId`, and UUID `IdempotencyKey` newtypes with parsing/Serde/schema support in `crates/cairn-domain/src/ids.rs` (FR-001, FR-009, FR-011)
- [X] T011 Add `Project`, `ProjectStatus`, and `ProjectRepositoryAssociation` models with duplicate-name, active/archive/restore, immutable identity, and repository-ID-only invariants in `crates/cairn-domain/src/project.rs` and exports in `crates/cairn-domain/src/lib.rs` (FR-001–FR-008)
- [X] T012 Add stable `Task` and immutable `TaskRevision` models with permanent project ownership, positive revision numbers, and same-task earlier-parent validation in `crates/cairn-domain/src/task.rs` (FR-009–FR-014)
- [X] T013 [P] Implement versioned `GoalContractV1` validation, CRLF/CR normalization, Unicode-edge trimming, fixed-order compact JSON, lowercase BLAKE3 fingerprinting, and the closed bounded `missing_required_field|malformed_structure|empty_goal|empty_list_entry|unsupported_version` violation union in `crates/cairn-domain/src/goal_contract.rs` (FR-015–FR-017, Constitution VI/VIII)
- [X] T014 [P] Extend `crates/cairn-domain/src/session.rs` with the closed `SessionBindingMode`/scope union `local_unbound|project_bound(project_id, task_revision_id)` while leaving `SessionState` and its transition matrix unchanged (FR-018–FR-019, Constitution II)
- [X] T015 Define focused project/task/binding validation errors and idempotency conflicts in `crates/cairn-project/src/error.rs`; retain one canonical wire mapping for `REPOSITORY_PROJECT_CONFLICT` and `SESSION_BINDING_CONFLICT` (FR-047, research R15)
- [X] T016 [P] Add ID, duplicate-name/title, project-status, permanent-ownership, and parent-validation unit tests in `crates/cairn-domain/tests/project_task.rs` (FR-001–FR-014)
- [X] T017 [P] Add canonical bytes/fingerprints, line endings, list order, empty-list acceptance, every required-field omission, malformed structure, empty goal, each list's empty-entry case, unsupported version, bounds, and non-leakage tests in `crates/cairn-domain/tests/goal_contract.rs` (FR-015–FR-017, SC-004)
- [X] T018 [P] Add the complete binding-mode × lifecycle-state independence matrix in `crates/cairn-domain/tests/session_binding_mode.rs`, proving scope changes do not create lifecycle transitions (FR-026, Constitution II)
- [X] T019 Add only explicit aggregate envelope/type declarations—aggregate type/ID, positive post-migration sequence, payload version dispatch, and derived event-key inputs—in `crates/cairn-events/src/aggregate.rs` and `catalog.rs`; story payload construction remains in T046/T062/T075/T087 (FR-037–FR-038, D1)
- [X] T020 Add deterministic derived per-event key tests from raw operation key + closed method + fixed event position, including task-create positions and bound-start stable session identity, in `crates/cairn-events/tests/idempotency.rs` (FR-038, G1)
- [X] T021 Add typed shared `Project`, association, task/revision, goal-contract, and discriminated session-scope DTO modules in `crates/cairn-protocol/src/project.rs`, `task.rs`, and `session.rs`; export them from `crates/cairn-protocol/src/lib.rs` (FR-046, contracts)
- [X] T022 Extend `crates/cairn-protocol/src/errors.rs` with the closed Feature 002 codes/data, including `PROJECT_SCOPE_REQUIRED`, `IDEMPOTENCY_CONFLICT`, `STORAGE_BUSY`, bounded `AMBIGUOUS_NAME`, all five goal violation kinds, and content-free migration data (FR-017, FR-047–FR-048, I1/U1/O1)

---

## Phase 3: SQLite Migration, Storage, and Aggregate Serialization

**Purpose**: Upgrade the exact Feature 001 database transactionally and make SQLite,
not a process-local lock, the correctness boundary.

**Checkpoint**: The real Feature 001 fixture migrates with zero loss; every new event
has real aggregate scope; migration and mutation failures expose no partial state.

- [X] T023 Implement production migration `crates/cairn-storage-local/migrations/0002_project_task_binding.sql` from the design SQL, including immutable `operation_idempotency`, and validate it through the actual SQLx runner rather than treating design DDL as production-ready (FR-033–FR-040, G1/G2/G3)
- [X] T024 Add typed project, association, task, revision, session-binding/mode, aggregate-event, and immutable operation-idempotency records in `crates/cairn-storage-local/src/records.rs` without retyping Feature 001 columns (FR-034, FR-038, data model)
- [X] T025 Extend `crates/cairn-storage-local/src/db.rs`, `apps/daemon/src/state.rs`, and startup routing so migration failure, recorded-checksum mismatch, or future schema version fails closed with bounded `MIGRATION_FAILED`, rolls back, and exposes no partially healthy daemon (FR-036, G2/G3)
- [X] T026 Add a closure-based `BEGIN IMMEDIATE` writer in `crates/cairn-storage-local/src/writer.rs` with `busy_timeout=5000ms`, zero application retries, 5000ms max elapsed, typed `STORAGE_BUSY`, rollback before pool reuse on error/cancellation, and a test-only timeout override (FR-040, O1)
- [X] T027 Implement `event_aggregate_heads` UPSERT/RETURNING allocation, global `events.seq` append, explicit aggregate validation, and operation-position-derived event keys in `crates/cairn-storage-local/src/aggregate_events.rs` (FR-038, G1)
- [X] T028 Update all post-migration Feature 001 event builders in `crates/cairn-storage-local/src/events.rs` and `crates/cairn-events/src/catalog.rs` to supply real repository/worktree/session aggregate scope while leaving stored legacy rows null and unchanged (FR-034, FR-038, compatibility matrix)
- [X] T029 Implement project and repository-association projection queries/writes, stable ID pagination, archive checks, and repository uniqueness in `crates/cairn-storage-local/src/projects.rs` (FR-001–FR-008)
- [X] T030 Implement task/revision projection queries, atomic latest-number advancement, immutable insertion, parent lookup, and rollback-safe gap-free allocation in `crates/cairn-storage-local/src/tasks.rs`; global retry resolution is T131 (FR-009–FR-017, O1)
- [X] T031 Implement session-scope lookup and one-row immutable binding projection writes in `crates/cairn-storage-local/src/session_bindings.rs`, preserving all existing session fields (FR-018–FR-026)
- [X] T032 Add test-only deterministic failure/cancellation controls in `writer.rs` and storage test support for post-registry-reservation, post-counter-allocation, pre/between-event, pre-projection, and pre-commit rollback checks (FR-040, G1/O1)
- [X] T033 [P] Test empty-database 0001→0002 migration, SQLx checksum/version recording, foreign keys, indexes, triggers, and `quick_check` in `crates/cairn-storage-local/tests/migration_0002_empty.rs` (FR-033, migration cases 1/9)
- [X] T034 [P] Migrate a copy of `fixtures/databases/feature-001-v1.sqlite3` and assert every historical field/hash/count is preserved in `crates/cairn-storage-local/tests/migration_0002_fixture.rs` (FR-034, SC-003)
- [X] T035 [P] Use the actual migration runner to test injected rollback/retry/second-open gating plus recorded checksum mismatch and database version newer than supported; require bounded non-leaking `MIGRATION_FAILED` and no healthy daemon in `crates/cairn-storage-local/tests/migration_0002_failure.rs` (FR-035–FR-036, G2/G3)
- [X] T036 [P] Assert every migrated session is historical `local_unbound` and no project, task, revision, association, binding, operation-idempotency record, aggregate head, or Feature 002 event is fabricated in `migration_0002_classification.rs` (FR-035, Constitution II/III)
- [X] T037 [P] Test FKs/indexes, archive guards, ownership, counters, parents, immutable/no-delete projections, and closed immutable operation-idempotency constraints in `migration_0002_constraints.rs` (FR-002, FR-006, FR-011–FR-013, FR-025, G1)
- [X] T038 [P] Test legacy-null/new-explicit aggregate scope, no fake IDs, contiguous suffix/global order, and duplicate derived event-key rejection in `events_aggregate.rs` (FR-038, SC-005)
- [X] T039 [P] With independent SQLx pools/connections, deterministically test aggregate order, lock exhaustion, cancellation, starvation bound, rollback after revision-counter allocation, and the next gap-free revision without a shared mutex in `concurrency.rs` (FR-038–FR-040, SC-006, O1)
- [X] T040 [P] Run existing repository/worktree/snapshot/session/event DAO cases against the migrated schema in `crates/cairn-storage-local/tests/feature001_compat.rs`, proving additive columns do not change Feature 001 reads/writes (FR-052, compatibility matrix)

---

## Phase 4: User Story 1 — Projects and Repository Association (Priority: P1) 🎯 MVP

**Goal**: Create, inspect, update, archive/restore projects and associate a stable Cairn
repository identity whose worktrees inherit membership.

**Independent Test**: Register a repository, create duplicate-named projects, associate
one project, move the repository directory, and verify the same IDs and association.

### Tests for User Story 1

- [X] T041 [P] [US1] Write failing project create/list/show/update/archive/restore and duplicate-name service tests in `crates/cairn-project/tests/project_lifecycle.rs` (US1-AS1–AS5, FR-001–FR-003)
- [X] T042 [P] [US1] Write failing association identity, same-pair retry, conflicting project, inherited-worktree membership, path-move, and prohibited transfer/removal tests in `crates/cairn-project/tests/repository_association.rs` (US1-AS6–AS10, FR-004–FR-008)
- [X] T043 [P] [US1] Write failing independent-connection concurrent project-update tests that assert one global raw-key winner/re-read or `IDEMPOTENCY_CONFLICT`, deterministic aggregate/global order, and projection equality in `project_concurrency.rs` (FR-038–FR-040, G1)
- [X] T044 [P] [US1] Write failing independent-connection competing association tests proving one registry/event/projection winner, stable association or idempotency conflict, and no partial/duplicate state in `association_concurrency.rs` (FR-006–FR-008, FR-040, G1)

### Implementation for User Story 1

- [X] T045 [US1] Implement project create/list/get/update, archive/restore, duplicate names, stable timestamps, and retries through the global operation registry in `project_service.rs` (FR-001–FR-003, G1)
- [X] T046 [US1] Implement typed `project.created`/full-state `project.updated`/association payloads, atomic projection writes, and their US1 replay handlers in `project_service.rs`, `catalog.rs`, and `replay.rs` (FR-037–FR-040, A2/D1)
- [X] T047 [US1] Implement repository-ID-only association, all-worktree membership lookup, same-pair idempotency, conflict rejection, and archived-project guard in `crates/cairn-project/src/project_service.rs` (FR-004–FR-008)
- [X] T048 [US1] Add typed `v1.project.create/list/get/update/repository_associate` params/results and bounded pagination in `crates/cairn-protocol/src/project.rs` and method constants in `crates/cairn-protocol/src/methods.rs` (FR-042, FR-046)
- [X] T049 [US1] Add project handlers and routing in `apps/daemon/src/handlers/projects.rs`, `apps/daemon/src/handlers/mod.rs`, and `apps/daemon/src/router.rs`; map domain failures to closed protocol errors (FR-042, FR-045, FR-047)
- [X] T050 [US1] Add `project create/list/show/update/repository add` Clap commands that call daemon IPC only in `apps/cli/src/commands/project.rs` and `apps/cli/src/commands/mod.rs` (FR-042, FR-045)
- [X] T051 [US1] Implement exact human project-name resolution with deterministic bounded candidates and ID-only JSON mode in `apps/cli/src/commands/project.rs` (FR-042, FR-047)
- [X] T052 [US1] Add project IPC schemas/goldens for exact same-key original `created/updated` flags (including after later project updates), distinct-key association `created:false`, cross-method/request conflicts, archive/restore, and errors in protocol schemas/goldens/tests (FR-046–FR-048, G1/SC-009)
- [X] T053 [US1] Add project CLI JSON goldens and exit-code assertions, including `IDEMPOTENCY_CONFLICT` exit 1, in `project_cli_contract.rs` and goldens (FR-042, FR-046–FR-048)
- [X] T054 [US1] Add daemon end-to-end coverage for repository registration, duplicate names, association, inherited worktrees, directory move, archive rejection/restore, retry, conflict, and absent transfer/remove commands in `apps/daemon/tests/feature002_projects.rs` (US1 independent test, SC-001)

**Checkpoint**: US1 is independently usable and testable; no task/session binding work is
required for its project/repository demonstration.

---

## Phase 5: User Story 2 — Immutable Tasks, Revisions, and Goal Contracts (Priority: P2)

**Goal**: Create stable tasks with immutable canonical revisions and serialize concurrent
revision allocation transactionally.

**Independent Test**: Create revision 1 and revision 2, retrieve both, and prove revision
1 bytes/fingerprint never change while the task points to revision 2.

### Tests for User Story 2

- [X] T055 [P] [US2] Write failing task-create, duplicate-title, revision-1 atomicity, later-revision, parent, latest/historical selection, archive rejection, and immutable-row tests in `crates/cairn-project/tests/task_revisions.rs` (US2-AS1–AS10, FR-009–FR-017)
- [X] T056 [P] [US2] Write failing service tests for canonicalization/fingerprint plus every `INVALID_GOAL_CONTRACT` variant/required field, malformed input, bounds, and absence of full-contract data in errors/logs in `goal_contract_service.rs` (FR-015–FR-017, I1)
- [X] T057 [P] [US2] With independent pools and barriers, test unique gap-free revisions, parents, deterministic timeout/cancellation, rollback after counter allocation, and the next successful number in `revision_concurrency.rs` (US2-AS5, SC-006, O1)
- [X] T058 [P] [US2] Write global same-key/method/request retry, cross-method/request conflict, and 100 pre/post immutable-read cases plus a configurable retry helper in `revision_acceptance.rs` (SC-002, SC-004, G1)

### Implementation for User Story 2

- [X] T059 [US2] Implement task + revision 1 + `task.created` then `task.revision_created` + projections in one global-idempotency transaction in `task_service.rs` (FR-009–FR-010, FR-037–FR-040, G1)
- [X] T060 [US2] Implement `BEGIN IMMEDIATE` gap-free revision allocation, parent validation, global retry/conflict resolution, latest advancement, and cancellation/error rollback in task service/storage (FR-011–FR-014, G1/O1)
- [X] T061 [US2] Integrate `GoalContractV1` and map exactly the five closed bounded content-free violations without echoing contracts in `goal_contract.rs` and `task_service.rs` (FR-015–FR-017, I1)
- [X] T062 [US2] Implement `task.created` and `task.revision_created` payloads containing the complete immutable revision plus complete resulting Task post-state (`latest_revision_number`, `updated_at` included), derived event keys, and US2 replay handlers in `catalog.rs`/`replay.rs` (FR-039, I2/A2)
- [X] T063 [US2] Add typed `v1.task.create/revise/list/get` params/results, revision selection, and bounded pagination in `crates/cairn-protocol/src/task.rs` and `crates/cairn-protocol/src/methods.rs` (FR-043, FR-046)
- [X] T064 [US2] Add task handlers and routing in `apps/daemon/src/handlers/tasks.rs`, `apps/daemon/src/handlers/mod.rs`, and `apps/daemon/src/router.rs` with atomic service calls and stable errors (FR-043, FR-045, FR-047)
- [X] T065 [US2] Add `task create/revise/list/show` CLI commands with goal contracts read from file/stdin and daemon IPC-only writes in `apps/cli/src/commands/task.rs` and `apps/cli/src/commands/mod.rs` (FR-043, FR-045, FR-051)
- [X] T066 [US2] Implement exact human task-title resolution within a required project, bounded candidates, and ID-only JSON mode in `apps/cli/src/commands/task.rs` (FR-043, FR-047)
- [X] T067 [US2] Add task IPC schemas/goldens for each required goal field missing, malformed structure, empty goal, every list empty-entry, unsupported version, bounded privacy data, retry/cross-key conflicts, parents/archive, and complete Task post-state in protocol tests/schemas/goldens (FR-017, FR-046–FR-048, I1/I2)
- [X] T068 [US2] Add task CLI goldens for all goal violations with exit 1/non-leakage, idempotency conflicts, complete Task post-state, historical selection, and stable envelopes in task CLI tests/goldens (FR-017, FR-043, FR-046–FR-048, I1)
- [X] T069 [US2] Add daemon end-to-end coverage for revision 1/2, duplicate titles, parent errors, archived restrictions, latest/historical reads, canonical fingerprints, and original-revision preservation in `apps/daemon/tests/feature002_tasks.rs` (US2 independent test, SC-004)

**Checkpoint**: US2 provides immutable project-owned goal scope; sessions are still
explicitly unbound until US3.

---

## Phase 6: User Story 3 — Bind Existing Bootstrap Sessions Explicitly (Priority: P3)

**Goal**: Bind one existing `local_unbound` session once without changing lifecycle
state, session identity, or earlier history.

**Independent Test**: Record a local session and its events, bind it, and observe exactly
one later `session.bound` event plus one immutable projection.

### Tests for User Story 3

- [X] T070 [P] [US3] Write failing active/recovering/stopped/interrupted binding tests that preserve lifecycle, IDs, snapshots, leases, timestamps, and pre-binding event hashes in `crates/cairn-session/tests/session_binding.rs` (US3-AS1–AS3/AS7, FR-020–FR-026)
- [X] T071 [P] [US3] Write failing repository/project mismatch, revision/project mismatch, archived project, conflicting bind, and prohibited unbind/rebind tests in `crates/cairn-session/tests/session_binding_errors.rs` (US3-AS4–AS8, FR-021, FR-025)
- [X] T072 [P] [US3] With independent pools/barriers, test identical/different binding races: one global registry result/event/projection, first commit winner, deterministic global order, and no partial state in `session_binding_concurrency.rs` (FR-024–FR-025, G1)

### Implementation for User Story 3

- [X] T073 [US3] Implement atomic bind—global registry resolution, relationship validation, aggregate allocation, `session.bound`, binding insert, mode update—in one transaction in `session_bindings.rs` (FR-020–FR-025, FR-040, G1)
- [X] T074 [US3] Implement `SessionService::bind` without lifecycle transitions or replacement sessions in `crates/cairn-session/src/binding.rs` and export it from `crates/cairn-session/src/lib.rs` (FR-020–FR-026, Constitution II/III)
- [X] T075 [US3] Implement typed `session.bound` with validated provenance, derived event identity, and the sole project-bound US3 replay transition in `catalog.rs` and `replay.rs` (FR-023–FR-024, I3/A2)
- [X] T076 [US3] Add typed `v1.session.bind` params/result and project-bound scope DTOs in `crates/cairn-protocol/src/session.rs` and `crates/cairn-protocol/src/methods.rs` (FR-044, FR-046)
- [X] T077 [US3] Add the session-bind handler and stable error mapping in `apps/daemon/src/handlers/session.rs` and `apps/daemon/src/router.rs` (FR-021, FR-044–FR-048)
- [X] T078 [US3] Add `cairn session bind` human/JSON output through daemon IPC in `apps/cli/src/commands/session.rs` (FR-029, FR-044–FR-045)
- [X] T079 [US3] Add bind IPC/CLI schemas/goldens for exact same-key original flags, distinct-key identical-binding `created:false`, idempotency/binding/mismatch/archive conflicts, and exit codes in protocol/CLI tests (FR-046–FR-048, G1)
- [X] T080 [US3] Add daemon restart and revision-2 tests proving persisted binding remains on revision 1 and earlier Feature 001 events remain byte-identical in `apps/daemon/tests/feature002_binding_restart.rs` (FR-031, FR-041, SC-001)
- [X] T081 [US3] Add deterministic identical-binding global-registry retry coverage and configurable loop helper for T113, asserting one registry record/projection/event at any retry count in `feature002_binding_retries.rs` (FR-024, SC-002, G1)

**Checkpoint**: Existing bootstrap sessions can be made project/task-aware explicitly;
there is still no unbind, rebind, or session-revision transition.

---

## Phase 7: User Story 4 — Start and Recover Correctly Scoped Sessions (Priority: P4)

**Goal**: Extend the one existing session-start path with explicit unbound/bound scope
while preserving every Feature 001 lifecycle and watcher guarantee.

**Independent Test**: Start a valid bound session, restart/recover the daemon, and verify
the same binding, revision, snapshot, lease, watcher, and event behavior.

### Tests for User Story 4

- [X] T082 [P] [US4] Write failing bootstrap-eligible explicit/omitted unbound starts, `PROJECT_SCOPE_REQUIRED` when selectable scope exists, bound starts, and repository/project/task/revision mismatches in `bound_start.rs` (US4-AS1–AS8, FR-027–FR-030, U1)
- [X] T083 [P] [US4] Write failing bound-start preservation tests for uniqueness, takeover, leases, resume-token hashing/non-leakage, snapshots, watcher readiness, post-install reconciliation, and interruption in `apps/daemon/tests/feature002_bound_start_invariants.rs` (FR-032, compatibility matrix)
- [X] T084 [P] [US4] Write failing healthy-collision tests for unbound↔bound and differing-bound scopes returning `SESSION_SCOPE_CONFLICT` without conversion in `crates/cairn-session/tests/bound_start_collisions.rs` (US4-AS7, FR-030)
- [X] T085 [P] [US4] Write a deterministic 20-cycle daemon restart/reattach harness for bound sessions in `apps/daemon/tests/feature002_bound_recovery.rs` (US4-AS5, SC-007)

### Implementation for User Story 4

- [X] T086 [US4] Extend session start/get/list DTOs with typed scope; decode omitted legacy scope as an unbound request subject to the same bootstrap eligibility/`PROJECT_SCOPE_REQUIRED` gate in domain/protocol session modules (FR-018–FR-019, U1)
- [X] T087 [US4] Extend the existing session creation transaction so `session.started` always creates replay-unbound, optional `session.bound` solely binds, both occur in order with projections, and none is visible before commit; add the US4 mixed replay handler in storage/events replay (FR-027, FR-039–FR-040, I3/A2)
- [X] T088 [US4] Extend—not duplicate—`SessionService::start` with project/revision validation, conditional unbound eligibility, `PROJECT_SCOPE_REQUIRED`, identical-scope collision reuse, and incompatible-scope rejection (FR-027–FR-032, U1)
- [X] T089 [US4] Preserve session transaction → watcher install → ready acknowledgement → authoritative reconciliation → success for both scopes; expose test barriers used by T134 without production sleeps in session/watch handlers (FR-032, Constitution I, A1)
- [X] T090 [US4] Preserve binding through recovering/reattach/interrupt/stop and watcher reinstallation, adding test-only recovery/reattach pause acknowledgements for T134 in recovery/session service (FR-022, FR-032, FR-041, A1)
- [X] T091 [US4] Add mutually exclusive `--local-unbound` versus paired project/revision flags and unambiguous session start/show/list rendering in `apps/cli/src/commands/session.rs` (FR-028–FR-029, FR-044)
- [X] T092 [US4] Add IPC goldens for omitted/explicit unbound in eligible and `PROJECT_SCOPE_REQUIRED` states, valid bound, mismatches/collisions, exact event semantics, and watcher install/reconcile errors in protocol tests/goldens (FR-030–FR-032, U1/I3)
- [X] T093 [US4] Add CLI JSON/human goldens and exit-code tests for both modes, explicit/omitted `PROJECT_SCOPE_REQUIRED`, and every start conflict in session-scope CLI tests/goldens (FR-029–FR-030, FR-046–FR-048, U1)
- [X] T094 [US4] Add barrier-driven watcher integration for installation-window/dropped/coalesced/failure cases and committed `session.started`→`session.bound`→interruption order, proving the first two are atomically invisible before commit, in `feature002_bound_start_watcher.rs` (FR-032, I3)

**Checkpoint**: Bound and unbound starts share one lifecycle implementation and the full
Feature 001 watcher/recovery suite remains green.

---

## Phase 8: Cross-Story IPC and CLI Contract Completion

**Purpose**: Close the complete method/error/envelope surface after US1–US4 methods
exist.

- [X] T095 Register every new/extended closed method in `crates/cairn-protocol/src/methods.rs` and `apps/daemon/src/router.rs`, rejecting unknown methods without weakening Feature 001 framing or security (FR-042–FR-046)
- [X] T096 Generate and intentionally check in JSON Schemas for every Feature 002 request/result/error and the additive session/events schemas under `crates/cairn-protocol/schemas/`; make `crates/cairn-protocol/tests/schemas.rs` fail on drift (FR-046, SC-009)
- [X] T097 Add tripwires for IDs, scope/status/stage, complete immutable revision and Task post-state, the five goal violation variants/bounds, `PROJECT_SCOPE_REQUIRED`, global idempotency, bounded ambiguity, typed errors, legacy omission eligibility, and Feature 001 goldens in `feature002_compatibility.rs` (FR-046, FR-052, I1/I2/G1/U1)
- [X] T098 Add a schema-valid golden for every stable code/closed data variant—including every goal violation, `PROJECT_SCOPE_REQUIRED`, `IDEMPOTENCY_CONFLICT`, `STORAGE_BUSY`, checksum/future migration failure—with no arbitrary JSON/content leakage in error goldens/tests (FR-017, FR-047–FR-048)
- [X] T099 Validate every new CLI command's success/failure `cairn.cli.v1` envelope, exactly-one-stdout-object rule, and exit codes 0/1/2/3/4/5/6 in `apps/cli/tests/feature002_json_stability.rs` (FR-046–FR-048, SC-009)
- [X] T100 Test `AMBIGUOUS_NAME` only as bounded internal human rendering, plus duplicate names/titles and exit-2 rejection/no IPC for every JSON-mode name selector, in `feature002_ambiguous_names.rs` (FR-047, D1)
- [X] T101 Extend `v1.events.list` DTO/handler/schema/goldens with paired aggregate filters and nullable-only-for-legacy aggregate fields in `crates/cairn-protocol/src/dto.rs`, `apps/daemon/src/handlers/events.rs`, and `crates/cairn-protocol/tests/events_contracts.rs` (FR-038–FR-039, contracts)
- [X] T102 Replay all Feature 002 goldens over a real Unix socket/named pipe and assert the CLI has no SQLx dependency or direct database writes in `apps/daemon/tests/feature002_ipc.rs`, `apps/cli/tests/feature002_ipc_only.rs`, and `apps/cli/Cargo.toml` (FR-045–FR-046, Constitution VII)

---

## Phase 9: User Story 5 — Migration, Replay, Privacy, and Offline Validation (Priority: P5)

**Goal**: Preserve all historical truth and rebuild Feature 002 projections from one
ordered ledger while operating fully offline.

**Independent Test**: Migrate the real Feature 001 fixture, create and bind Feature 002
state, restart, replay into empty projections, and compare every stable field.

### Tests and Implementation for User Story 5

- [X] T103 [US5] Integrate the already story-owned replay handlers into strict mixed-ledger verification; add corruption/unknown-version handling without introducing first-time project/task/binding handlers or rewriting Feature 001 replay in `replay.rs` (FR-039, A2)
- [X] T104 [P] [US5] Add mixed legacy/new field-for-field equality for projects/status/associations, full Tasks (latest number/updated_at), revisions, bindings, and lifecycle; assert started=unbound/bound-event-only semantics in `feature002_replay.rs` (US5-AS4, SC-005, I2/I3)
- [X] T105 [P] [US5] Test unknown versions/types, aggregate gaps, fabricated project/task worktree IDs, invalid binding references, and projection mismatch failures in `crates/cairn-events/tests/feature002_replay_invalid.rs` (edge cases, Constitution I/III)
- [X] T106 [P] [US5] Inject failure/cancellation after global registry reservation, counter allocation, each event/projection boundary for every mutation/bound start; assert zero partial registry/head/event/projection state in `feature002_atomicity.rs` (US5-AS5, G1/O1)
- [X] T107 [P] [US5] Exercise the real and representative large Feature 001 fixtures through migration, second open, restart, and zero-loss manifest comparison in `apps/daemon/tests/feature002_migration_acceptance.rs` (US5-AS1–AS3, SC-003)
- [X] T108 [US5] Automate reordered Quickstart 1–12—bootstrap before project/task, then `PROJECT_SCOPE_REQUIRED`, bind/restart/revise/replay—with exact counts and earlier-event hashes in `feature002_quickstart.rs` (SC-001, U1)
- [X] T109 [P] [US5] Add privacy sentinels for goal fields, ignored contents, environment values, resume tokens, internal paths, and migration/SQL causes; audit stdout/stderr/logs/errors/DB in `apps/daemon/tests/feature002_privacy.rs` and the CLI stdout/stderr/envelope half in `apps/cli/tests/feature002_privacy_cli.rs` (FR-048, FR-051, SC-011)
- [X] T110 [P] [US5] Add a prebuilt-binary offline acceptance harness that proves external network denial plus local filesystem/Git/IPC success in `scripts/ci/feature002-network-isolated.sh` (FR-049–FR-050, SC-008)
- [X] T111 [US5] Add a Linux CI job that fetches/builds before isolation, uses `unshare -n` or a fail-closed `--network none` container fallback, and runs migration/quickstart/replay through `scripts/ci/feature002-network-isolated.sh` in `.github/workflows/ci.yml` (FR-049, SC-008)
- [X] T112 [P] [US5] Add the 100-project/1,000-task/five-revision fixture and p95 create/list/show/revise/associate/bind measurements with the two-second threshold in `apps/daemon/tests/feature002_perf.rs` (SC-010)
- [X] T113 [P] [US5] Run 100 identical association/revision/binding retries through the global registry; assert one registry result/logical event/projection for each and conflicts for cross-method/request key reuse in `feature002_retry_acceptance.rs` (SC-002, G1)

**Checkpoint**: Replay equals live projections, migration loses nothing, privacy is clean,
and the full flow succeeds with external networking disabled.

---

## Phase 10: Frozen-Commit Cross-Platform and Full-Flow Evidence

**Purpose**: Collect real completed evidence without confusing workflow configuration
with execution.

- [X] T114 Configure exact-SHA Windows/macOS/Linux/isolation, Feature 002 performance, exactly-100 Feature 001 kills, and explicit Feature 001 SC-007 ignored-perf jobs with uploaded commands/counters/measurements in `.github/workflows/ci.yml`; configuration alone completes no execution task (FR-053, E1)
- [X] T115 Run the pre-freeze local quality/Feature 001 focused/Feature 002 acceptance gate and record exact outputs in `evidence/pre-freeze.md`; this is readiness, not exact-SHA platform or inherited acceptance evidence (FR-052, Constitution IV)
- [X] T116 Create one clean frozen Feature 002 implementation commit after T115; record SHA/tree/status/toolchain in `implementation-freeze.json` and never amend/rebase/add runtime changes to it (evidence strategy)
- [X] T117 [P] From a clean macOS checkout of T116, run Quickstart 1–12 plus formatting, Clippy, workspace tests, platform IPC/path/restart/SQLite cases, and record commands/results/event counts in `specs/002-project-task-binding/evidence/macos.md` (FR-053, SC-001/SC-012)
- [X] T118 [P] Complete the Windows exact-SHA CI job for T116, including named-pipe security, path moves, migration, restart, SQLite, Quickstart, and quality commands; record run/job references and outputs in `specs/002-project-task-binding/evidence/windows.md` (FR-053, SC-012)
- [X] T119 [P] Complete the normal Linux exact-SHA CI job for T116, including Unix-socket, migration, restart, SQLite, Quickstart, and quality commands; record run/job references and outputs in `specs/002-project-task-binding/evidence/linux.md` (FR-053, SC-012)
- [X] T120 [P] Complete the Linux OS-level network-isolated exact-SHA job for T116 with explicit external-denial and local filesystem/IPC proofs; record mechanism, commands, scenarios, and job result in `specs/002-project-task-binding/evidence/linux-isolated.md` (FR-049, SC-008)
- [X] T121 [P] Execute the SC-010 performance target on T116, recording OS/architecture, fixture cardinalities, per-operation measurements, p95 values, thresholds, and pass/fail in `specs/002-project-task-binding/evidence/performance.md` (SC-010)
- [X] T122 After T117–T121 and T135–T136 complete, consolidate the frozen implementation SHA, completed job references, platforms/toolchains, scenarios/counts, privacy/offline/Feature 002 performance, exactly-100-kill counters, SC-007 measurements/limits, and quality results in preliminary `evidence/acceptance-summary.md`; reject configured/skipped/dirty/different-SHA claims (SC-001–SC-012, E1)
- [X] T123 Commit only completed preliminary execution evidence after T122 as a separate evidence commit without amending T116; documents record the implementation SHA but never require their own eventual commit SHA (testing evidence, E1)

---

## Phase 11: Final Quality and Convergence Gate

**Purpose**: Verify—not assume—that all specification, compatibility, execution, and
scope gates are satisfied.

- [ ] T124 Audit macOS/Windows/Linux/isolation/100-kill/SC-007 evidence against the one T116 SHA, including exact commands, counters/measurements, environments, completed jobs, and quality results; rerun missing work instead of inferring it in `evidence/final-gate.md` (FR-052–FR-053, E1)
- [ ] T125 Verify SC-001–SC-012 plus exactly 100 forced kills with zero committed-event/state loss and explicit `cargo test -p cairn-daemon --test perf -- --ignored` measurements/limits from completed T116 runs; record every result in `final-gate.md` (Constitution IV, E1)
- [ ] T126 Audit the frozen implementation tree for no Feature 001 regression, no direct CLI SQLite writes, and no server synchronization, PostgreSQL, accounts/roles, continuity, MCP, context/AI memory, truth, embeddings, drift, product completion-verification, web UI, or Feature 003 changes; record paths inspected in `specs/002-project-task-binding/evidence/scope-audit.md` (FR-050, out-of-scope list, Constitution IX)
- [ ] T127 After the preliminary evidence commit, run `/speckit-analyze` for Feature 002 and record CRITICAL/HIGH/MEDIUM findings; leave incomplete while any severe inconsistency remains (Constitution I/IV, E1)
- [ ] T128 Run `/speckit-verify-run` then `/speckit-verify-tasks-run`; record results/phantom completions and reopen every unsupported task before convergence (Constitution IV, E1)
- [ ] T129 Run `/speckit-converge`; resolve and revalidate every genuinely appended implementation/evidence task, rerunning analyze/verify as needed, and remain incomplete until convergence has no gap (completion gate, E1)
- [ ] T130 Run only after T001–T129, T131–T136, and any T129 convergence-appended tasks are complete (135/136 before this task on the current ledger); produce authoritative final accounting, final-gate report, and final evidence/declaration commit, then mark the resulting current ledger 136/136—or the larger resulting denominator if convergence appended work. Never require the committed document to contain its own SHA; record it through workflow metadata, an annotated tag, or a later external manifest (SC-001–SC-012, Constitution IV, E1)

### Appended remediation tasks (execute at their owning dependency phase)

- [X] T131 Implement the global immutable raw-key registry in `crates/cairn-storage-local/src/operation_idempotency.rs` and all mutation paths: method/fingerprint; immutable event locator for appended operations or immutable association/binding locator for distinct-key no-ops; exact original flags/post-state; cross-method/request `IDEMPOTENCY_CONFLICT`; one registry/events/projections transaction (FR-038–FR-040, G1)
- [X] T132 Add independent-connection/process tests proving first commit wins, exact original flags/state after later mutable updates, distinct-key association/binding false results, restart reread, cross-method/request conflict, and all-or-none registry/events/projections across `crates/cairn-storage-local/tests/concurrency.rs`, `crates/cairn-project/tests/{project_concurrency,association_concurrency,repository_association}.rs`, `crates/cairn-session/tests/{session_binding_concurrency,session_binding_atomicity}.rs`, `apps/cli/tests/session_bind_cli_contract.rs`, and `apps/daemon/tests/feature002_retry_acceptance.rs` (FR-040, SC-002/SC-006, G1)
- [X] T133 Establish the typed Feature 001+002 replay dispatcher in `crates/cairn-events/src/replay.rs` before T041/US1; it must reject unsupported Feature 002 payload versions and provide registration points consumed by T046/T062/T075/T087 without implementing story payloads early (FR-039, A2/D1)
- [X] T134 Add barrier-controlled integration tests in `apps/daemon/tests/feature002_binding_races.rs` for binding while watcher reconciliation is paused and while recovery/reattach is paused; assert lifecycle/binding independence, exactly one binding result, deterministic global order, and no duplicate/partial registry/event/projection (FR-022–FR-026, SC-006, A1)
- [X] T135 [P] Execute the frozen T116 SHA with exactly `CAIRN_CRASH_ITERS=100 CAIRN_CRASH_EXPECTED_ITERS=100 cargo test -p cairn-daemon --test us4_crash_restart -- --nocapture`; record SHA/environment/command, configured=100, completed=100, zero committed-event/state loss, and pass/fail in `evidence/feature001-100-kill.md`—configuration or another SHA is not evidence (FR-052–FR-053, E1)
- [X] T136 [P] Execute `cargo test -p cairn-daemon --test perf -- --ignored` at frozen T116; record SHA/OS/architecture/toolchain, tracked-file fixture size, inspect/snapshot durations, SC-007 limits, and pass/fail in `evidence/feature001-sc007.md`—an ignored workspace result is not evidence (FR-052–FR-053, E1)
---

## Dependencies and Execution Order

### Phase dependencies

1. **Phase 1** has no implementation dependency; T001–T007 gate T008–T009.
2. **Phase 2** depends on Phase 1; T133 establishes the replay dispatcher after Phase 2 types and before T041/US1.
3. **Phase 3** depends on Phase 2. T131 implements the global registry after T023–T027, and T132 proves it; T023–T040 plus T131–T133 block every story mutation.
4. **US1 / Phase 4** depends on that storage/replay foundation and adds project payload/replay handlers.
5. **US2 / Phase 5** depends on US1 and adds task/revision payload/replay handlers.
6. **US3 / Phase 6** depends on US1 + US2 + Feature 001 sessions and adds binding replay.
7. **US4 / Phase 7** extends the single start path; T134 runs after binding plus T089/T090 test controls.
8. **Phase 8** depends on US1–US4 method implementations.
9. **US5 / Phase 9** integrates already-present handlers and depends on T134 plus Phases 3–8.
10. **Phase 10** depends on all implementation/local acceptance through T115 and T134. T117–T121 and T135–T136 execute only at immutable T116 and may run concurrently.
11. **Preliminary evidence** T122–T123 depends on every exact-SHA execution. T127 analyze → T128 verify/verify-tasks → T129 converge/remediation follows.
12. **T130** runs last, only after T001–T129 and T131–T136 are complete; it is not its own prerequisite.

### Critical path

```text
T001–T007 baseline
→ T008–T022 domain/protocol + T133 replay dispatcher
→ T023–T040 migration/storage + T131 registry + T132 concurrency proof
→ T041–T054 US1 replayable projects
→ T055–T069 US2 replayable tasks
→ T070–T094 US3/US4 binding/start + T134 lifecycle races
→ T095–T113 contracts and mixed acceptance
→ T114–T116 CI support/readiness/frozen SHA
→ T117–T121 + T135–T136 exact-SHA executions
→ T122–T123 preliminary evidence
→ T124–T129 audit/analyze/verify/converge/remediation
→ T130 authoritative 136/136 final declaration
```

No later phase may compensate for a failed earlier migration, compatibility, or
append-only-history gate.

## Safe Parallel Opportunities

- After T010, T013–T014 can proceed in parallel; tests T016–T018 use separate files.
- After T023–T032 and T131, storage tests T033–T040 and T132 use independent files/pools.
- T133 must complete before US1, but story tests T041–T044 can then be written concurrently.
- US2 tests T055–T058 and US3 tests T070–T072 are internally parallel where marked.
- US4 tests T082–T085 can run concurrently; T134 waits for T089/T090 controls.
- After story handlers exist, mixed replay T104–T107 and privacy/offline/performance/retry targets T109–T113 can be distributed.
- After T116 freezes one SHA, T117–T121 and T135–T136 may run concurrently; evidence is not interchangeable.

Tasks sharing `crates/cairn-protocol/schemas/`, `.github/workflows/ci.yml`, daemon
routing, or a service implementation file are intentionally not marked parallel.

## Acceptance and Evidence Gates

| Gate | Required tasks |
|---|---|
| Feature 001 compatibility | T001–T007, T040, T083, T094, T115, T124, T135–T136 |
| Migration/negative runner/zero loss | T023, T025, T033–T037, T107, T125 |
| Global operation idempotency | T020, T023–T027, T032, T043–T044, T058, T072, T106, T113, T131–T132 |
| SQLite contention/cancellation | T026, T032, T039, T057, T060, T106 |
| Goal validation contracts/privacy | T013, T017, T022, T056, T061, T067–T068, T097–T099, T109 |
| Immutable revision/read preservation | T055–T069, T080, T104, T125 |
| Bootstrap scope/bound-start/recovery races | T082–T094, T108, T125, T134 |
| Replay completeness/no fake scope | T046, T062, T075, T087, T101, T103–T108, T125, T133 |
| Typed IPC/CLI compatibility | T048–T053, T063–T068, T076–T079, T086–T102 |
| Real offline isolation | T110–T111, T120, T125 |
| Feature 002 performance | T112, T121, T125 |
| Feature 001 100-kill + SC-007 | T114, T124–T125, T135–T136 |
| Cross-platform exact-SHA/preliminary evidence | T114–T123, T135–T136 |
| Final analyze/verify/converge/declaration | T124–T130 |

## Implementation Strategy

### MVP first

The smallest independently useful Feature 002 slice is Phases 1–4:

1. preserve and freeze Feature 001 behavior;
2. establish typed/storage foundations;
3. migrate safely;
4. deliver project creation and repository association (US1);
5. stop and run the US1 independent test before adding task or binding behavior.

### Incremental delivery

1. **US1**: project and repository scope.
2. **US2**: immutable task-revision and goal-contract scope.
3. **US3**: explicit one-time binding for existing sessions.
4. **US4**: correctly scoped new sessions through the unchanged start lifecycle.
5. **US5**: combined migration/replay/offline proof.
6. Freeze one implementation SHA, collect real platform evidence, then converge.

## Scope guardrails

No task authorizes central synchronization, PostgreSQL, user accounts/membership,
roles, repository transfer/removal, cross-device continuity, MCP, context compilation,
AI memory, truth claims, embeddings, drift detection, product completion verification,
web UI, multi-agent orchestration, session unbind/rebind/revision transition, or
Feature 003 implementation.
