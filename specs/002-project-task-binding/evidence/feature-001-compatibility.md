# Feature 001 compatibility decision record

- Baseline checkout: `fb204b07d6c8ee4e1f82fce262feb311086df551`
- Frozen fixture producer: `4a06c4125715bb4b78b54e49c81eccd82100a7b7`
- Date: 2026-07-22
- Decision: **PASS — T001–T007 compatibility gate permits foundational Feature 002 work**

## Compatibility mapping

| Inherited behavior | Passing evidence |
|---|---|
| Repository/worktree identity and inspection | Workspace `us1_register_inspect` and `cairn-git` status/fingerprint suites |
| Deterministic snapshots | Workspace `cairn-domain` fingerprint and `cairn-git` fingerprint-determinism suites |
| Session uniqueness and lifecycle | Workspace `us2_sessions`, session-state matrix, and `us2_agent_sim` |
| Lease authentication and token hashing | Workspace `us2_sessions`, `us4_recovery`, CLI session contract, and privacy suites |
| Watcher readiness and post-install reconciliation | Focused `us3_tracking`: 10/10, including installation-window deletion, dropped notification, coalescing, deduplication, restart, and install failure |
| Append-only events and deterministic order | Focused `us3_events`: 2/2 plus storage append/rollback/concurrency tests |
| Replay | Workspace `cairn-events` replay tests: 2/2 |
| Crash/recovery behavior | Workspace `us4_crash_restart` and `us4_recovery`: all default-configuration tests passed |
| Secure local IPC | Workspace daemon integration suites over the platform socket and protocol serialization tests |
| CLI JSON contracts | Workspace CLI contract and JSON-stability suites |
| Privacy and secret containment | Workspace `log_redaction` and `privacy_audit` suites |

## Fixture and schema decision

The fixture was generated by clean, detached binaries from the frozen Feature 001
commit. It has migration version 1, four repositories/worktrees/snapshots/sessions,
18 ordered events, one session in every Feature 001 lifecycle state, and no Feature
002 table or column. The fixture SHA-256 and every manifest count/hash verify through
`feature001_fixture`.

No unexplained baseline, schema, or fixture mismatch remains. Migration 0001 was not
edited, legacy events were not rewritten, and no project/task/binding/aggregate
history was fabricated.
