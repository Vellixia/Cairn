# V1 baseline inventory

Frozen at `37417192f316cb495601edf44d5bddfa967b0e5c` before Task 2 files. Run `bash scripts/v1-baseline.sh`; it verifies frozen SHA exists, prints immutable frozen measurements, then prints current-worktree comparison. It is read-only and measures source partitions, mechanical surface counts, crate graph, and local artifact sizes. Production = Rust `crates/*/src/*.rs` plus web `app/components/hooks/lib` TS/TSX. Tests, fixtures, generated output, dependencies never count as production reduction.

Measured source baseline: 82,783 Rust production LOC + 9,256 web production LOC = 92,039 production LOC. Excluded: 26,669 inline `#[cfg(test)]` LOC (modules, functions, impls, imports, statics), 91,452 external Rust-test LOC, 1,786 web-E2E LOC, 3,104 fixture/corpus LOC; `target/`, `web/node_modules/`, and `web/.next/` are dependencies/generated output. Rust-aware lexer masks rustdoc, comments, normal strings, raw strings, and braces before locating guarded items; `scripts/v1-rust-loc.py --self-check` covers each form and times out malformed lexing.

Current comparison: 84,687 Rust production LOC + 9,267 web production LOC = 93,954 production LOC, a net increase of 1,915. Required graph, replay, analytics, typed-contract, and clean-break transfer code outweighed duplicate client/presentation reductions. Tests, fixtures, and generated output contribute zero production-reduction credit. This is a measured miss against the reduction objective, not reported as consolidation savings.

## Surface inventory / V1 disposition

| Surface | Inventory | V1 |
|---|---|---|
| CLI primary | `init`, `status`, `agents`, `connect`, `doctor`, `repair`, `disconnect`, `context`, `link`, `unlink`, `traits`, `update`, `mcp`, `hook` | keep core setup/status/context/capture; simplify discovery (`setup` replaces `init`, grouped help) |
| CLI grouped | `integration {export,migrate,distribute}`; `session {list,checkpoint,start,show,end}`; `task {list,show,new,set-status,update,criterion {add,set,verify,remove},blocker {open,clear},readiness,history}`; `memory {pin,add,supersede,subject,reinforce,reconcile,search,show,forget}`; `evidence {add,list,show}`; `handoff show`; `privacy {exclude,unexclude,list}`; `delete {observation,memory,handoff,session}`; `pattern {list,show,promote,outcome,forget}`; `verify`; `migrate`; `sync {status,now}`; `daemon {logs,start,stop,status}` | keep capabilities; simplify compact groups; replace direct top-level `verify`, `pattern`, `privacy`, `delete`, `link/unlink`, `traits` with grouped discovery |
| CLI admin | `auth {token set,logout,status,change-password}`; `user {create,list,disable,enable,promote,demote,reset-password}`; `team {list,propose,ratify,retire}`; `personal {list,forget}`; `project member {add,remove,list}` | keep authorization boundaries; simplify administration/governance groups |
| MCP | `cairn_context`, `cairn_search`, `cairn_remember`, `cairn_session`, `cairn_task`, `cairn_handoff` | keep exactly six; replace extra tools with typed actions |
| HTTP | 62 declarations in `crates/cairn-server/src/{api,events}.rs`: health/version, auth/admin/tokens, projects/members, sync/events, memories/relations/commands/patterns/global knowledge, migration, web reads, verification, retrieval/trace, session/memory detail | keep domain authorization; simplify presentation duplicates only; remove `POST /api/auth/register` and project `join` only as existing 410 stubs |
| Web pages | `/login`, `/`, `/admin/users`, `/system`, `/team`, `/tokens`, project overview/activity/agents/domains/memory/list+detail/retrieval/list+detail/sessions/list+detail/sync/tasks | replace project nav with Overview, Memory, Sessions/Replay, Tasks; keep governance/admin global; remove standalone activity/agents/domains/sync/retrieval after panels land |
| PostgreSQL schema | 5 migrations: users/session/token/project/task/memory/handoff/sync; relations/criteria/blockers; server identity/personal/team knowledge + applicability + relations; autonomous-memory additions; team revision; `schema_migrations` | keep canonical schema; simplify only after exporter/importer preservation tests; no datastore replacement |
| SQLite schema | 12 migrations: edge project/task/session/memory/evidence/handoff/outbox; integrations; relations/criteria/patterns; sync/global domains; safe-event + command spools; cache/authority/version | keep pending state/cache; simplify legacy dual-authority only after migration proof |
| Jobs | daemon sync drainer; 15-minute daemon recovery/idle-session/handoff/verification sweep; server consolidator with bounded trace retention; request-scoped capture/delivery | keep bounded jobs; simplify via existing ticks; no worker platform |

## Dependencies, duplicates, quality

Crate graph: `cairn-core`, `cairn-git`, `cairn-sys` leaves; `cairn-store` + `cairn-integrate` → core; `cairnd` → core/git/integrate/store; `cairn` → core/git/integrate; `cairn-server` → core/integrate; `cairn-e2e` test-only → core/store/sys/integrate. Process graph: `cairn`/hooks/MCP → `cairnd` + SQLite → Axum + PostgreSQL; web consumes server API. No consolidation justified: boundaries isolate edge state, adapters, canonical authorization, UI.

Duplicate evidence: CLI/HTTP share server command/auth paths; `integration-health` delegates `read_health`; server account CLI calls `auth::create_user`; MCP forwards to daemon. Task 4 must mechanically re-check validation, privacy, authorization, ranking, serialization, identity, scope seams. Moving code earns zero reduction.

Retrieval quality is separate from latency: committed SQLite harness `crates/cairn-store/src/search.rs` supplies 3 scoped candidates for query `tests` and asserts expected rank task → branch → project despite higher project lexical relevance (3/3 expected positions); it also asserts punctuated lexical recall `cargo test --workspace` (1/1). Run `RUSTC=/Users/andresholivin/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin/rustc /Users/andresholivin/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin/cargo test -p cairn-store search::tests --lib`. Latency test `tests/tests/feature005_retrieval_performance.rs` remains separate. PostgreSQL evidence `tests/feature005/performance-measurements.md`: capture median 23.18–23.72 ms/worst 41.74 ms (250 ms); session-open median 40.92–43.96/worst 93.26 ms (250 ms); prompt median 41.07–45.58/worst 74.85 ms after one 102.98 ms contention outlier (100 ms); 10k backlog 0.93–1.19x.

`scripts/v1-baseline.sh` is checked-in per-item inventory: its `per-item inventory` section prints `surface|source|item|keep/simplify/remove/replace` for every CLI enum action, HTTP route (including typed `.web_operation` registry entries), page, PostgreSQL and SQLite/FTS table, MCP tool/actions, and job. Registry routes are counted as `raw .route` plus `.web_operation` entries, so source text cannot undercount live browser endpoints. Every emitted inventory item carries intentional disposition; frozen values never change when planned current work changes a surface. PostgreSQL/SQLite sets include programmatic `schema_migrations`, extracted from `db.rs`/`migrate.rs` alongside migrations. `Verify`, `Link`, and `Unlink` are `replace`; `Pattern`, `Privacy`, and `Delete` are `simplify`. Measured web `.next`: 222,371,840 bytes. Release command: `RUSTC=/Users/andresholivin/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin/rustc /Users/andresholivin/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin/cargo build --release --bins`; artifacts: `cairn` 9,121,968 bytes, `cairnd` 17,950,032 bytes, `cairn-server` 10,598,976 bytes. Script gives portable `cargo +1.97.1` fallback.
