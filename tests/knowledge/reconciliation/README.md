Cases for `derive_subject` and for the one automatic merging rule.

Automatic reconciliation covers **exactly one** merging case: content that is identical after
normalization (`contracts/knowledge.md` §Automatic reconciliation, D46). Every subdirectory here
exists to hold that line in place from one side or the other.
