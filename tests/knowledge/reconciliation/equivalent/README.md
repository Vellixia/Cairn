≥20 cases whose members are identical after normalization and therefore **must** reconcile to one
canonical answer.

Rule: every case asserts `reconciliation: reinforced` (or `settled` for a single member), exactly one
entry in `answers`, and a `duplicates` relation recorded new→existing with `basis: deterministic_rule`.

Every case here has a sibling in `../distinct/` differing in exactly the way that matters (SC-301).
