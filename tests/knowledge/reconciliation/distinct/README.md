≥20 **paired negatives** for `../equivalent/`. Superficially similar, materially different, and
therefore must **not** merge.

Rule: no case may record a `duplicates` or `reinforces` relation, and every member stays active and
retrievable. A case here is only useful if its sibling in `../equivalent/` would merge — the pair is
the measurement (SC-301 metric 2).
