≥15 **adversarial** cases: one topic key, one value key, materially different statements.

    auth.strategy=jwt   "JWT uses HS256 with a shared secret."
    auth.strategy=jwt   "JWT uses RS256 with rotating public keys."

Rule: every case asserts `reconciliation: corroborated`, **zero** relations recorded, and every
statement retained and retrievable. An equal value key states agreement about the *value*; it never
establishes that two differently-worded statements are one claim (FR-327, D77).

This directory is the corpus form of the false-merge path R12 closed. A case here that yields
`reinforced` is a defect, not a fixture to update.
