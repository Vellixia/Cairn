Reusable cross-project patterns: promotion, refusal, independence accounting and counterexamples.

A pattern is a separate local record, never a widened memory scope, and it never synchronizes
(FR-391, FR-508, D61).
