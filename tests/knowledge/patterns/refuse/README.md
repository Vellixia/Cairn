One case per refusal class, ten classes, in the gate's fixed order so the reported reason is stable.

Rule: the refusal **names the class**, echoes **no value**, and leaves **no partial pattern** (FR-397).
Two classes deserve their own mention: `attested_not_sufficient` and `imported_not_sufficient` are the
two ways an agent could otherwise launder its own claim into cross-project knowledge (SC-328).
