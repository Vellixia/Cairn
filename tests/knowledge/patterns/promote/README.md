Candidates that pass all ten gate checks.

Rule: the source is lifecycle `active`, `verified` with authority **`cairn`**, carries ≥1 evidence
fact, is not `local_only`, belongs to a subject that is not conflicted, is of a transferable type,
survives the deterministic privacy check, defines ≥`pattern_signals_min` signals, and duplicates no
existing pattern (FR-396).
