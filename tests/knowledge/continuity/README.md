Ten-cycle compaction traces with field-presence assertions **per cycle**.

Rule: after every one of ten consecutive compaction cycles, every FR-422 field that exists in
recorded state is still delivered, and none of it is a paraphrase of a conversation (SC-310). The
checkpoint is derived from recorded project state, never from a provider's summary.
