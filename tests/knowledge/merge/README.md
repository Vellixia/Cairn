Two-store offline scenarios, **each with a clock-reversed twin**.

Rule: reversing the relative clock order of the two stores produces a byte-identical merged canonical
state and an identical conflict set (SC-304). A case whose outcome moves under clock reversal has
found a timestamp arbitration, which FR-303 and FR-411 forbid.
