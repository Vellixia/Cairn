Memory populations 0 · 10 · 500 · 5,000 crossed with budgets 200 … 12,000.

Rule: `estimated_tokens <= budget` in 100% of assemblies, and at the documented minimum budget with
5,000 memories the Tier 0a guaranteed work state is complete (SC-308, SC-309). The population-0 case
additionally asserts the no-regression property: with no task, no warnings, no pins and no checkpoint
the briefing is unchanged from the pre-feature baseline in `../baseline/`.
